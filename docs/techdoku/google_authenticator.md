# google authenticator

```
sudo google-authenticator --time-based --disallow-reuse --force --label="test2@somewhere" --quiet --rate-limit=2 --rate-time=60 --secret=/etc/apache2/ga_auth/test2 --window-size=17
```
